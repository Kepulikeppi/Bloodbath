export const RoomTypes = {
    // Standard flat room
    NORMAL: 'NORMAL',
    
    // Room with a sunken central area (Death pit or lower arena)
    PIT: 'PIT',
    
    // Room with a raised central platform
    PODIUM: 'PODIUM',
    
    // Room with a chasm in the middle but a bridge crossing it
    BRIDGE: 'BRIDGE'
};